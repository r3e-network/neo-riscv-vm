[package]
name = "devpack-test"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
neo-riscv-devpack = { path = "../../crates/neo-riscv-devpack" }
polkavm-derive = "0.32.0"

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
