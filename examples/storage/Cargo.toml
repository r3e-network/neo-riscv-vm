[package]
name = "storage"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
neo-riscv-devpack = { path = "../../crates/neo-riscv-devpack" }
neo-riscv-abi = { path = "../../crates/neo-riscv-abi" }

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
